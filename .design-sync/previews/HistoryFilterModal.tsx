import HistoryFilterModal from '@/components/modal/HistoryFilterModal';

// Reads/writes the history filter store; defaults render an unfiltered panel.
export const Default = () => <HistoryFilterModal />;
