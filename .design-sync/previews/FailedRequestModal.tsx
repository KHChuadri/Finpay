import FailedRequestModal from '@/components/modal/FailedRequestModal';

// Uses useNavigate — DsRouter provider supplies the router.
export const Default = () => <FailedRequestModal onClose={() => {}} />;
